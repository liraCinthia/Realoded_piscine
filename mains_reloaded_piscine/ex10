#include <stdio.h>

void    ft_swap(int *a, int *b)
{
        int     c;

        c = *a;
        *a = *b;
        *b = c;
}

int main()
{
        int a = 10;
        int b = 42;

        ft_swap(&a, &b);

        printf("valor de a:%d valor de b:%d\n", a, b);
        return (0);
}

